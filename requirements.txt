# BAIF Anuwad Studio — runtime dependencies only
# Target: Python 3.12 x64 (Windows) or arm64/x64 (macOS).
# These packages are required for normal offline translation. Build a matching
# wheelhouse with this file when preparing the no-internet USB installation.

streamlit==1.32.0
faster-whisper==1.0.1
ctranslate2==4.1.0
sentencepiece==0.2.0
sherpa-onnx==1.13.4
soundfile==0.12.1
srt==3.5.3
numpy==1.26.4
